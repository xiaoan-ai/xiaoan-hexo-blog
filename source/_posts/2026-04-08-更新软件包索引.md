---
title: 更新软件包索引
date: 2026-04-08T12:00:00+08:00
categories:
  - 安全工具
tags:
  - 工具
---

## 1. 下载安装Ollama

1、访问Ollama官网（https://ollama.com/），下载ollama启动器，并点击安装

![image-20241026191030323](/images/notes/杂项-使用Ollama与open-webui搭建AI大模型知识库/images/1.png)

2、安装完成后，Win+R，输入cmd，打开cmd命令行，输入ollama并回车，显示如下则表明安装成功

![image-20241026191039051](/images/notes/杂项-使用Ollama与open-webui搭建AI大模型知识库/images/2.png)

3、配置环境变量，编辑系统环境变量Path，新建一行，输入Ollama安装位置

![image-20241026191049899](/images/notes/杂项-使用Ollama与open-webui搭建AI大模型知识库/images/3.png)



## 2. 下载并配置大模型

### 2.1 修改环境变量

调整下载的大模型存储位置（比较大，默认存在C盘，很占位置）；新建系统环境变量：**OLLAMA_MODELS**,路径自定义到你想存放的地址即可，点击确认并保存

### 2.2 **重启Ollama**

**一定要重启，否则上述修改不生效**。右下角点击ollama图标，quit退出，之后再重新打开cmd命令行，输入Ollama启动即可

### 2.3 下载需要的大模型

访问ollama官网的模型仓库（https://ollama.com/library），选择想安装的模型，比如阿里千问：qwen2.5，点击模型，选取版本（训练数据量不同）

![image-20241026191127819](/images/notes/杂项-使用Ollama与open-webui搭建AI大模型知识库/images/4.png)

复制该命令，回到cmd命令行，键入该命令并回车，之后耐心等待下载完毕即可

### 2.4  简单使用

1、命令行输入命令：ollama run 模型名 以启动模型  示例：

```
ollama run ollama run qwen2.5:7b  （qwen2.5:7b是阿里集团千问模型的名字）
ollama run deepseek-r1  （部署7b大小的deepseek-r1模型）
```

 2、ollama list 查看当前拥有的模型列表（不记得模型名时可以使用，方便启动）
 3、ollama -h  查看使用命令帮助

**下载完成，启动之后即可键入任意内容，开启对话**

### 2.5 配置局域网访问环境变量

ollama的API默认只允许本地环回地址访问，也即：

```
http://127.0.0.1:11434/
http://localhost:11434/
```

我们需要让局域网主机也能访问，或者本机局域网ip也能访问，需要额外进行配置

#### 2.5.1 linux下

Ollama的默认配置是只有本地才可以访问，需要配置一下：

```shell
找到配置文件：/etc/systemd/system/ollama.servic

添加 Environment="OLLAMA_HOST=0.0.0.0:11434"

结果文件如下：

...
[Service]
Environment="OLLAMA_HOST=0.0.0.0:11434"
ExecStart=/usr/bin/ollama serve
...
重启：

systemctl daemon-reload
systemctl restart ollama
这样，在外网就能够使用ip的方式进行访问。
```

#### 2.5.2 windows下

Ollama提供了两个API访问接口，常用的是Chat。两个API连接默认是127.0.0.1:11434，也即默认只能用本机访问11434端口，可以在浏览器中看到如下效果：

![image-20241119204429247](/images/notes/杂项-使用Ollama与open-webui搭建AI大模型知识库/images/5.png)

共有两个变量控制，一个OLLAMA_HOST控制访问地址，一个OLLAMA_PORT控制端口

如笔者这里需要设置为局域网可访问，且端口需要修改为11434（其他任意端口行），则对应的变量应该设置OLLAMA_HOST为0.0.0.0，OLLAMA_PORT为11434：

![image-20241119204703005](/images/notes/杂项-使用Ollama与open-webui搭建AI大模型知识库/images/6.png)

**1. 配置之后记得重启ollama才能生效！！！！**

**2. 重启记得重开cmd窗口进行启动该，否则环境变量不生效！！！！！**

### 2.6 拓展配置

> **Ollma**可以像其他软件一样在电脑上完成一键安装，不同的是，建议按照实际需求配置下系统环境变量参数

| 参数                     | 标识与配置                                                   |
| ------------------------ | ------------------------------------------------------------ |
| OLLAMA_MODELS            | 表示模型文件的存放目录，默认目录为**当前用户目录**即 `C:\Users%username%.ollama\models` Windows 系统 **建议不要放在C盘**，可放在其他盘（如 `E:\ollama\models`） |
| OLLAMA_HOST              | 表示ollama 服务监听的网络地址，默认为**127.0.0.1**  如果想要允许其他电脑访问 Ollama（如局域网中的其他电脑），**建议设置**成 **0.0.0.0** |
| OLLAMA_PORT              | 表示ollama 服务监听的默认端口，默认为**11434**  如果端口有冲突，可以修改设置成其他端口（如**8080**等） |
| OLLAMA_ORIGINS           | 表示HTTP 客户端的请求来源，使用半角逗号分隔列表 如果本地使用不受限制，可以设置成星号 `*` |
| OLLAMA_KEEP_ALIVE        | 表示大模型加载到内存中后的存活时间，默认为**5m**即 5 分钟 （如纯数字300 代表 300 秒，0 代表处理请求响应后立即卸载模型，任何负数则表示一直存活） 建议设置成 **24h** ，即模型在内存中保持 24 小时，提高访问速度 |
| OLLAMA_NUM_PARALLEL      | 表示请求处理的并发数量，默认为**1** （即单并发串行处理请求） 建议按照实际需求进行调整 |
| OLLAMA_MAX_QUEUE         | 表示请求队列长度，默认值为**512** 建议按照实际需求进行调整，超过队列长度的请求会被抛弃 |
| OLLAMA_DEBUG             | 表示输出 Debug 日志，应用研发阶段可以设置成**1** （即输出详细日志信息，便于排查问题） |
| OLLAMA_MAX_LOADED_MODELS | 表示最多同时加载到内存中模型的数量，默认为**1** （即只能有 1 个模型在内存中） |

> 默认的模型保存路径在C盘，我们把路径更改到有更多可用空间的分区或目录，可以更好地管理存储空间。

- 添加 `OLLAMA_MODELS` 环境变量改变模型存储的位置。例如 `E:\ollama\models`
- 设置完成后重启 `ollama` 或 `PowerShell`，即可完成更改。

## 4. 安装open-webui

### 4.1 docker方法安装

#### 4.1.1 安装docker

##### **问题1：ubuntu20的vmtools失效**

**解决：**

```shell
sudo apt-get autoremove open-vm-tools
sudo apt-get install open-vm-tools
sudo apt-get install open-vm-tools-desktop
```

为方便管理，采用ubuntu20虚拟机，安装docker来使用open-webui

```shell
# 更新软件包索引
sudo apt-get update
 
# 安装需要的软件包以使apt能够通过HTTPS使用仓库
sudo apt-get install ca-certificates curl gnupg lsb-release
```

- 使用清化大学源

```bash
# 添加Docker官方的GPG密钥
curl -fsSL https://mirrors.tuna.tsinghua.edu.cn/docker-ce/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
 
# 设置稳定版仓库
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://mirrors.tuna.tsinghua.edu.cn/docker-ce/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
```

- 使用阿里云源

```csharp
# 添加阿里云官方GPG密钥
curl -fsSL http://mirrors.aliyun.com/docker-ce/linux/ubuntu/gpg | sudo apt-key add -
 
# 写入阿里云Docker仓库地址
sudo sh -c 'echo "deb [arch=amd64] http://mirrors.aliyun.com/docker-ce/linux/ubuntu $(lsb_release -cs) stable" > /etc/apt/sources.list.d/docker.list'
```

更新源并安装Docker

```sql
sudo apt-get update
sudo apt-get install docker-ce docker-ce-cli containerd.io

# 验证是否成功安装了docker
sudo systemctl status docker
docker --version
```

修改docker的`/etc/docker/daemon.json`配置文件，如果在不存在则手动创建，文件内容如下。

```bash
# 修改daemon.json文件，
vim /etc/docker/daemon.json

# daemon.json内容如下：
{
    "registry-mirrors": [
        "https://dockerproxy.com",
        "https://docker.m.daocloud.io",
        "https://cr.console.aliyun.com",
        "https://ccr.ccs.tencentyun.com",
        "https://hub-mirror.c.163.com",
        "https://mirror.baidubce.com",
        "https://docker.nju.edu.cn",
        "https://docker.mirrors.sjtug.sjtu.edu.cn",
        "https://github.com/ustclug/mirrorrequest",
        "https://registry.docker-cn.com"
    ]
}

# 重载配置文件，并重启 docker
sudo systemctl daemon-reload
sudo systemctl restart docker

# 查看 Registry Mirrors 配置是否成功
sudo docker info 
```

#### 4.1.2 docker 安装open-webui

**1、安装及启动**

```bash
sudo ufw allow 3000  //放行3000端口

docker run -d -p 3000:8080 -e OPENAI_API_KEY=your_secret_key -v open-webui:/app/backend/data --name open-webui --restart always ghcr.io/open-webui/open-webui:main

命令详解：
docker run：这个命令用于创建并运行一个新的Docker容器。
-d：表示容器将在后台运行（detached模式）。
-p 3000:8080：将主机的3000端口映射到容器的8080端口。这样你可以通过访问主机的3000端口来访问容器内的服务。
-e OPENAI_API_KEY=your_secret_key：设置环境变量OPENAI_API_KEY为your_secret_key。这个环境变量通常用于将API密钥传递给应用程序。
-v open-webui:/app/backend/data：创建一个名为open-webui的数据卷，并将其挂载到容器内的/app/backend/data路径。这样可以在主机和容器之间共享数据，并在容器重启或重新创建时保留数据。
--name open-webui：为容器指定一个名称open-webui，这样你可以更方便地管理和引用这个容器。
--restart always：设置容器的重启策略为“始终重启”。如果容器意外停止或Docker服务重启，容器会自动重启。
ghcr.io/open-webui/open-webui:main：指定要运行的镜像和标签。在这里，使用的是GitHub Container Registry上的open-webui镜像，标签为main。
```

##### 问题2：ubuntu磁盘容量不足

```bash
1. 关闭虚拟机，管理，扩展磁盘容量
2. 追加分配扩展的磁盘
参考：https://blog.csdn.net/tcjy1000/article/details/135349729
```

扩容之后，再重新安装open-webui即可。

此时，访问虚拟机3000端口即可打开open-webui

```
http://192.168.8.130:3000/
```

##### 注意1：进入之后注册的第一个用户即为管理员用户！！！

#### 4.1.3 open--webui配置拉取前面的qwen2.5

此时，直接访问docker启动的open-webui会发现没有模型可以使用，需要配置模型源

右上角打开管理员设置，找到外部连接设置，添加我们的局域网本地主机配置的模型地址

```
http://192.168.8.1:11434
```

![image-20241119211707209](/images/notes/杂项-使用Ollama与open-webui搭建AI大模型知识库/images/7.png)

此时，再回到首页，即可发现，已经将前面配置的qwen2.5模型加载出来了

### 4.2 windows 本地docker安装

此种方法需要开启windows Hyper-V功能，会与虚拟机Vmware冲突，因此暂不进行试验。

### 4.3 采用python进行安装

**建议使用python3.11进行安装**

```bash
#安装
python311 -m pip install open-webui
#查看帮助
open-webui help
#启动服务
open-webui serve

之后访问8080端口即可
```

## 5. 配置化AI知识库

### 5.1 文档设置

如果需要使用RAG文档识别功能则需要配置此项

![image-20241119214319328](/images/notes/杂项-使用Ollama与open-webui搭建AI大模型知识库/images/8.png)

RAG提示词：

```text
Generate Response to User Query

Step 1: Parse Context Information
Extract and utilize relevant knowledge from the provided context within <context></context> XML tags.

Step 2: Analyze User Query
Carefully read and comprehend the user's query, pinpointing the key concepts, entities, and intent behind the question.

Step 3: Determine Response
If the answer to the user's query can be directly inferred from the context information, provide a concise and accurate response in the same language as the user's query.

Step 4: Handle Uncertainty
If the answer is not clear, ask the user for clarification to ensure an accurate response.

Step 5: Avoid Context Attribution
When formulating your response, do not indicate that the information was derived from the context.

Step 6: Respond in User's Language
Maintain consistency by ensuring the response is in the same language as the user's query.

Step 7: Provide Response
Generate a clear, concise, and informative response to the user's query, adhering to the guidelines outlined above.

User Query: [query]
<context>[context]</context>
```

### 5.2 自定义模型

自定义模型是将特定提示词、外部资料、特定功能集成在一起的AI组合模型，相较于原生API通用模型，它会更加定向于某个专长，从而更好的帮助用户解决实际问题。自定义模型并不是真正意义上的训练模型，只是给基础模型做了一部分能力集成，如果原来你接触过GPTs将会非常容易理解，比如Python学习专家、情感分析师、日语翻译官等等。

#### 5.2.1 创建知识库

![image-20241119220118733](/images/notes/杂项-使用Ollama与open-webui搭建AI大模型知识库/images/9.png)

#### 5.2.2 准备知识库资料

准备你的知识库资料，可以是文本文档、word文档都行，pdf也可尝试，（需要在设置中开启OCR识别）。

这里测试，我准备了一个gpt生成的一个公司简介文件

![image-20241119220449147](/images/notes/杂项-使用Ollama与open-webui搭建AI大模型知识库/images/10.png)

在工作空间，知识库中，将我们准备的数据上传到知识库中

![image-20241119221614641](/images/notes/杂项-使用Ollama与open-webui搭建AI大模型知识库/images/11.png)

#### 5.2.3 创建自定义模型，挂载知识库

创建自定义模型，模型任意选择一个chat模型，并挂载刚才上传的知识库。

![image-20241119221118663](/images/notes/杂项-使用Ollama与open-webui搭建AI大模型知识库/images/12.png)

**系统提示词**：

```
你是一个专业的写作助手，专门负责撰写工作总结、工作日报、工作周报、政府公文、思想汇报和各种书面文件。请遵循以下规则：
1. 格式要求：生成的文档应具有规范、正式的格式，符合各类正式文书的写作标准。结构清晰，条理分明。每个部分应有适当的标题，并遵循公文写作的标准格式。段落简洁有序，避免长篇大论。内容应分段处理，段落内部内容要有逻辑递进，段落之间要有自然过渡。文档内容需清晰、直观，避免杂乱无章的排版。
2. 语言表达：请确保所写内容语言流畅、正式、规范，避免使用口语或不适合正式场合的表达。简洁明了的语句可以提高文件的易读性，但不失专业性。语言应准确、明确、无歧义。避免不必要的修饰词，避免冗余句子和词汇的重复，确保每个句子都简洁而有力。要避免过于复杂或不常用的术语，尽量使内容通俗易懂，但又要保证专业性。
3. 内容要求：您生成的内容应当完整、有逻辑性，并紧扣主题。在总结或报告中，确保突出重点，明确表达工作成果、问题与解决方案等。各个部分应紧密关联，突出重点，清晰地表述每个阶段的工作内容、问题及解决措施。对于项目或任务的总结，应包括背景分析、执行过程、问题解决方案以及结果评估等要素，确保结论清晰明确、数据准确可信。
4. 避免假信息：如果您对某个问题不确定或无法提供准确答案，请说明原因，而不是生成虚假的信息。不要猜测或编造未确认的事实，避免错误信息影响决策。确保内容有据可依，避免随意推测。对于未知问题，应避免给出模糊或虚假的回答，而是提供您能确定的信息或告知需要进一步核实的情况。
5. 连贯性和合理性：如果提出的问题或任务缺乏实际意义，或表达不清，您应解释原因，而不是提供不连贯或无意义的回答。如果任务的要求模糊或缺少必要的信息，主动询问澄清或提出合理的假设，并确保最终内容的准确性和相关性。应特别注意段落和内容的衔接，确保整体文档的逻辑性和顺畅度，避免出现脱节或不连贯的部分。
6. 文体规范：在撰写公文或报告时，遵循政府文件的规范要求。避免过于冗长或复杂的语言，确保文体正式、简洁、清晰。公文应使用标准格式，如标题、正文、结尾、签名等，并确保用词严谨、语言规范。避免使用花哨的修饰语，保证内容的严谨和正式。对于正式文件，需特别注意格式、标点和措辞的准确性，以符合公文写作标准。
7. 具体要求：在处理任务时，确保对每个请求的具体需求做出回应，适当使用标题、段落和编号，使文档结构更加清晰、易于理解。文件内容要根据不同任务的具体要求进行调整。例如，工作总结应包括成果展示、问题分析与解决措施，而思想汇报则侧重思想总结与个人感悟。每一部分都要根据任务的需求进行适当的拓展或缩减，确保响应准确。
8. 审查与校对：在完成文档生成后，请自我审查内容的准确性、逻辑性和格式。确保没有遗漏关键点，没有语法或拼写错误。特别是在撰写政府公文时，要确保使用标准的公文写作规范，如日期格式、编号规则等，避免文中出现任何表述上的偏差。细心检查文件中的所有数据和信息，确保其准确无误。若有疑问或不确定的部分，应及时向相关部门或人士确认。
9. 适应性与灵活性：不同类型的文件可能有不同的需求，您应根据具体文件类型调整写作风格和内容。例如，工作总结应强调过去的工作成果和反思，而思想汇报则更注重个人思想的表达与总结。在撰写政府公文时，应特别注意文体的正式性和逻辑性，避免使用过于生动或感性的语言，确保用语得体，条理清晰。
10. 时间敏感性：在编写工作总结或报告时，考虑到时间的要求，确保突出当前阶段的关键问题和进展，适当展示未来的工作计划或改进措施。特别是在年终总结或专项报告中，要对整体工作进度做出明确的时间性评估。对于进展报告或阶段性报告，要清晰展示工作推进的时间节点，确保任务进度、完成情况的及时反映。
11. 创新与实用性：在编写报告或文件时，适当加入创新性思维和实用的建议。尽管文体需要严谨，但在某些报告中，可以加入具有前瞻性和实用价值的思考，尤其是针对未来的工作方向或改善措施。特别是在涉及政策或战略的报告中，要注意突出实际可行性和创新性，避免过度理想化的内容。
12. 综合性：处理文件时，要全面收集相关数据、信息和意见，确保所有相关领域的因素都得到了充分的考虑。无论是工作总结、政府公文，还是思想汇报，都要保证内容的全面性、客观性与实用性，避免片面或忽略关键信息的情况。
13. 特别要求：请务必确保内容具有高度的专业性与精确性，避免过于简略或空洞的表述。对于工作总结，尤其要注重结果的呈现与分析，强调任务完成的实效性。对任何不明确的问题，要提出合理的解释或进一步核实，而不是盲目给出回答。若遇到工作中的技术性难题或政策性内容时，请提供专业的解决方案或合理的建议。
```

填入提示词，保存之后，即可新建对话进行测试

![image-20241119222249497](/images/notes/杂项-使用Ollama与open-webui搭建AI大模型知识库/images/13.png)

### 5.3 安装pipeline（**暂时用不到，先略过**）

```bash
如果你需要完整的使用OpenWebUI中的工具和函数，还需要安装此项目，如此以来就可以实现诸如以下的功能：

自定义RAG
各种函数调用
实施监控和分析消息
有害信息过滤
速率限制
…
安装仅需在服务器执行以下命令（首先开启9099端口）
docker run -d -p 9099:9099 --add-host=host.docker.internal:host-gateway -v pipelines:/app/pipelines --name pipelines --restart always ghcr.io/open-webui/pipelines:main
```



## 6. 测试与优化

### 6.1 系统提示词优化

待续

### 6.2 知识库RAG优化

待续

### 6.3 open-webui界面美化

#### 6.3.1 明确目标

```
1、调整聊天界面配色，放弃自带的纯白或者纯黑主题配色，需要警用深蓝配色
或
2、调整背景图模糊度，让背景图清晰显示
```



#### 6.3.2 定位文件

```
1.f12调试前端源码，发现控制配色的源码位置
```

![image-20241120222432459](/images/notes/杂项-使用Ollama与open-webui搭建AI大模型知识库/images/14.png)

```css
style="--color-gray-800: #333; --color-gray-850: #262626; --color-gray-900: #171717; --color-gray-950: #0d0d0d;"
```

下一步，我们的open-webui是通过

```python
python311 -m pip install open-webui  进行安装的
因此项目源代码存在的位置应该是默认放在Python 环境对应的 site-packages 目录中
如果找不到，可以通过：
1、检查 pip 的安装记
pip show open-webui

2、python输出
import open_webui
import os

print(os.path.dirname(open_webui.__file__))
等方式找到
我的在：X:\xxx\xxx\xxx\python311\Lib\site-packages下
```

接下来，使用pycharm打开open-webui项目，右上角全局搜索上述关键字

![image-20241120223612395](/images/notes/杂项-使用Ollama与open-webui搭建AI大模型知识库/images/15.png)

定位到文件为index.html,进入到该文件，查找该文件路径，发现为：

```
python311/Lib/site-packages/open-webui/frontend/index.html
```

接下来，就对定位到的文件进行修改即可。

#### 6.3.3 修改配色

```
python311/Lib/site-packages/open-webui/frontend/index.html文件中：

style="
    --color-gray-800: #001f3f; 
    --color-gray-850: #001b36; 
    --color-gray-900: #00172d; 
    --color-gray-950: #000f20;
"
```

此时，重新刷新页面，设置**开启暗色主题**，即可得到如下效果

![image-20241120235741140](/images/notes/杂项-使用Ollama与open-webui搭建AI大模型知识库/images/16.png)

但此时，上传背景图，效果仍是遮盖，显示模糊：如下

![image-20241120235837897](/images/notes/杂项-使用Ollama与open-webui搭建AI大模型知识库/images/17.png)

#### 6.3.4 修改背景遮盖度

通过burpsuite抓包，上传背景图却抓不到包，怀疑图片根本没有直接上传，而是做成了Base64编码嵌入在前端代码中，因此f12审查源码，找到背景图片掩盖位置：

![image-20241121001131100](/images/notes/杂项-使用Ollama与open-webui搭建AI大模型知识库/images/18.png)

因此尝试删除

![image-20241121001304907](/images/notes/杂项-使用Ollama与open-webui搭建AI大模型知识库/images/19.png)

发现成功让背景图清晰显示

接下来，复制这段遮盖代码，定位，修改即可

```
dark:from-gray-900 dark:to-[#171717]
```

大概是这两个文件，我修改过了，缩短搜索条件，这里找出来多几个文件，如果没修改过，直接搜索应该会只有两个文件

两个文件的路径位于：

```
python311\Lib\site-packages\open_webui\frontend\_app\immutable\nodes下
```

![image-20241121001821223](/images/notes/杂项-使用Ollama与open-webui搭建AI大模型知识库/images/20.png)

```
python311\Lib\site-packages\open_webui\frontend\_app\immutable\chunks
```

![image-20241121100711696](/images/notes/杂项-使用Ollama与open-webui搭建AI大模型知识库/images/21.png)

修改之后，记得保存，然后刷新页面，发现，背景图片已经清晰显示了。

![image-20241121002112634](/images/notes/杂项-使用Ollama与open-webui搭建AI大模型知识库/images/22.png)

效果达成，收工！

### 6.4 注意 ！！！

注意：只能暗色主题生效！！！

## 7.向内网已经部署好的该系统中添加新模型办法

1、在外部可联网机器上，**执行本教程1、2步，安装好ollama支撑环境及需要的模型之后**，对模型进行打包转移。方法如下：

2、在文件系统中找到ollama存放模型文件的文件夹，名称为：（**Ollama_models**）,其下有两个子文件夹，（**blobs与manifests**）,将Ollama_models文件夹进行完整压缩，然后克隆到内网系统电脑上，替换内网电脑上的该同名文件夹，并重启ollama服务即可。

3、注意：如此替换会导致内网原有模型消失，需重新按上述教程操作。